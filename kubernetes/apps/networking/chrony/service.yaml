apiVersion: v1
kind: Service
metadata:
  name: chrony-udp
  namespace: networking
  labels:
    app: chrony
spec:
  ports:
    - name: ntp-udp
      port: 123
      protocol: UDP
      targetPort: 123
  selector:
    app: chrony
    tier: frontend
  type: LoadBalancer
  loadBalancerIP: "10.17.8.5"
